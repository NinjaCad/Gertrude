{
  "name": "CIS322E_SP26",
  "image": "therealhoneybadger/tmu_cis322:sp2026",
  "workspaceFolder": "/app",
  "workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-pyright.pyright"
      ]
    }
  },
  "postCreateCommand": "git config --global user.name '<Name>' && git config --global user.email '<masters_email>' && git config --global alias.dog 'log --decorate --all --oneline --graph'"
}